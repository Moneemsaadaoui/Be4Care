<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="rrdl.be4care">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/hand"
        android:label="@string/app_name"
        android:roundIcon="@drawable/hand"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".Views.Activities.SignUpActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Views.Activities.MainActivity"
            android:taskAffinity="" />
        <activity android:name=".Views.Activities.WelcomeActivity" />
        <activity android:name=".Views.Activities.AddDocumentActivity" />
        <activity
            android:name=".Views.Activities.LoginActivity"
            android:label="Be4Care"
            android:noHistory="true"
            android:taskAffinity="" />
        <activity android:name=".Views.Activities.ProfileActivity" />
        <activity android:name=".Views.Activities.DoctorDetail" />
        <activity android:name=".Views.Fragments.CompleteProfileActivity" />
        <activity android:name=".Views.Activities.DocumentDetails" />
        <activity
            android:name=".Views.Activities.SplashActivity"
            android:noHistory="true"
            android:taskAffinity=""
            android:theme="@style/Theme.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Views.Activities.DocInfoActivity" />
        <activity android:name=".Views.Activities.MapActivity"></activity>
    </application>

</manifest>